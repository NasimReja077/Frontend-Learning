# **Ultimate Tailwind CSS Curriculum (Beginner → Advanced)**

---

## **1. Introduction**

1. What is Tailwind CSS?
2. Utility-first vs traditional CSS
3. Advantages and disadvantages
4. How Tailwind CSS works
5. Setting up Tailwind: CDN vs NPM
6. JIT mode explained
7. PurgeCSS and production optimization

**Example:**

```html
<button class="bg-blue-500 text-white px-4 py-2 rounded">Click Me</button>
```

---

## **2. Colors**

8. Default color palette
9. Text colors (`text-red-500`)
10. Background colors (`bg-green-200`)
11. Border colors (`border-blue-500`)
12. Gradient colors (`bg-gradient-to-r from-red-500 to-yellow-500`)
13. Custom colors in tailwind.config.js
14. Opacity with colors (`bg-red-500/50`)

---

## **3. Typography**

15. Font size (`text-sm`, `text-lg`)
16. Font weight (`font-bold`, `font-light`)
17. Font family (`font-sans`, `font-serif`)
18. Line height (`leading-loose`)
19. Letter spacing (`tracking-wide`)
20. Text alignment (`text-center`, `text-right`)
21. Text decoration (`underline`, `line-through`)
22. Text transform (`uppercase`, `lowercase`, `capitalize`)
23. Font style (`italic`, `not-italic`)
24. Responsive typography (`sm:text-lg md:text-xl`)

---

## **4. Spacing**

25. Margin (`m-4`, `mt-2`, `mx-auto`)
26. Padding (`p-6`, `px-4`, `py-2`)
27. Gap for flex/grid (`gap-4`)
28. Negative margin (`-mt-2`, `-ml-4`)
29. Space between items (`space-x-4`, `space-y-2`)

---

## **5. Sizing**

30. Width (`w-full`, `w-1/2`, `w-64`)
31. Height (`h-10`, `h-screen`)
32. Min/max width & height (`min-w-full`, `max-h-96`)
33. Aspect ratio (`aspect-w-16 aspect-h-9`)

---

## **6. Borders**

34. Border width (`border`, `border-2`)
35. Border color (`border-red-500`)
36. Border radius (`rounded`, `rounded-lg`, `rounded-full`)
37. Border style (`border-dashed`, `border-dotted`)
38. Divide utilities (`divide-x`, `divide-y`)

---

## **7. Backgrounds**

39. Solid colors (`bg-blue-500`)
40. Gradients (`bg-gradient-to-r`)
41. Background position (`bg-center`)
42. Background size (`bg-cover`, `bg-contain`)
43. Background repeat (`bg-no-repeat`)

---

## **8. Flexbox**

44. Flex container (`flex`)
45. Flex direction (`flex-row`, `flex-col`)
46. Justify content (`justify-start`, `justify-center`)
47. Align items (`items-start`, `items-center`)
48. Align content (`content-start`, `content-center`)
49. Flex wrap (`flex-wrap`, `flex-nowrap`)
50. Flex grow/shrink/basis (`flex-grow`, `flex-shrink`, `basis-1/2`)
51. Space between items (`space-x-4`, `space-y-4`)

---

## **9. Grid**

52. Grid container (`grid`)
53. Grid columns (`grid-cols-3`)
54. Grid rows (`grid-rows-3`)
55. Gap between items (`gap-4`)
56. Auto-fit & auto-fill (`grid-cols-auto`)
57. Grid template areas (custom layouts)
58. Responsive grid (`sm:grid-cols-2 md:grid-cols-4`)

---

## **10. Positioning**

59. Position property (`static`, `relative`, `absolute`, `fixed`, `sticky`)
60. Top, right, bottom, left (`top-0`, `left-1/2`)
61. Z-index (`z-10`, `z-50`)
62. Object positioning (`object-center`, `object-cover`)

---

## **11. Display & Visibility**

63. Display types (`block`, `inline-block`, `hidden`)
64. Overflow (`overflow-auto`, `overflow-hidden`)
65. Visibility (`visible`, `invisible`)
66. Screen reader only (`sr-only`)

---

## **12. Pseudo-classes**

67. Hover (`hover:bg-blue-500`)
68. Focus (`focus:ring-2 focus:ring-blue-500`)
69. Active (`active:bg-blue-700`)
70. Disabled (`disabled:opacity-50`)
71. Group hover (`group-hover:text-red-500`)
72. First & last child (`first:mt-0`, `last:mb-0`)
73. Odd/even (`odd:bg-gray-100`, `even:bg-gray-200`)

---

## **13. Transitions & Animations**

74. Transition properties (`transition`, `transition-colors`)
75. Duration (`duration-300`)
76. Timing functions (`ease-in`, `ease-out`)
77. Keyframe animations (`animate-spin`, `animate-bounce`)
78. Custom animations using `@keyframes`

---

## **14. Shadows & Effects**

79. Box shadows (`shadow`, `shadow-lg`)
80. Opacity (`opacity-50`)
81. Ring utilities (`ring-2`, `ring-offset-4`)
82. Blur & filters (`blur-sm`, `brightness-125`)

---

## **15. Responsive Design**

83. Breakpoints (`sm`, `md`, `lg`, `xl`, `2xl`)
84. Mobile-first design
85. Conditional classes (`md:bg-red-500`)
86. Hiding/showing elements (`hidden md:block`)

---

## **16. Dark Mode**

87. Dark mode setup (`darkMode: 'class'`)
88. Dark mode utility (`dark:bg-gray-800 dark:text-white`)
89. Switching themes with JS

---

## **17. Forms**

90. Input styling (`border p-2 rounded w-full`)
91. Select, textarea, checkbox, radio styling
92. Focus ring for forms (`focus:ring-2`)
93. Disabled/readonly styling
94. Placeholder styling (`placeholder-gray-400`)

---

## **18. Customization**

95. tailwind.config.js
96. Extending theme (colors, fonts, spacing)
97. Adding custom breakpoints
98. Adding plugins
99. Using `@apply` for reusable classes

---

## **19. Advanced Utilities**

100. Transform (`rotate-45`, `scale-110`)
101. Skew & translate (`-translate-x-1/2`)
102. Filters (`grayscale`, `contrast-150`)
103. Line clamp (`line-clamp-3`)
104. Aspect ratio (`aspect-w-16 aspect-h-9`)

---

## **20. Components & Projects**

105. Buttons & links
106. Cards
107. Modals & Popups
108. Navbar & Sidebar
109. Dashboard layouts
110. Masonry / Pinterest-style gallery
111. Landing pages & hero sections
112. E-commerce UI
113. Forms & form validation UI
114. Responsive tables

---
